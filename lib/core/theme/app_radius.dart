class AppRadius {
  AppRadius._();

  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 12.0;
  static const double lg = 16.0;
  static const double xl = 24.0;
  static const double xxl = 32.0;
  static const double pill = 9999.0;

  // Semantic Feature Aliases
  static const double card = lg; // 16.0 for elegant cards
  static const double sheet = xl; // 24.0 for bottom sheets & modals
  static const double button = pill; // Pill shaped for primary actions
  static const double chip = pill; // Pill shaped for badges & tags
}
